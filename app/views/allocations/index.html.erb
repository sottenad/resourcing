<div class="row">
	<div class="col-sm-10">
		<h2>Allocations</h2>	
	</div>
	<div class="col-sm-2">
		<%= link_to "New Allocation", new_allocation_path, :class=>"btn btn-primary btn-sm" %>
	</div>
</div>




<div class="row">	
		<div class="gridSlider">
			<!--Calendar -->
		</div>

</div>



<script id="sliderTemplate" type="text/x-handlebars-template">	
	{{#each .}}
		<div class="userRow row">
			<div class="col-sm-3">
				<div class="userBlock">
					{{user.display_name}}
				</div>
			</div>
			<div class="col-sm-9">
				<div class="userAllocations">
						{{#each user.allocations}}
							<div class="userAllocation" data-start="{{allocation.start_date}}" data-end="{{allocation.end_date}}" data-duration="{{allocation.duration_in_days}}">
								{{allocation.project.title}}
							</div>
						{{/each}}
					</ul>
				</div>
			</div>
		</div>
	{{/each}}
</script>

