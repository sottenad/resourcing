<%= simple_form_for(@subscription_type) do |f| %>
  <%= f.error_notification %>

  <div class="form-inputs">
    <%= f.input :subscription_name %>
    <%= f.input :price %>
  </div>

  <div class="form-actions">
    <%= f.button :submit %>
  </div>
<% end %>
