<!DOCTYPE html>
<html>
  <head>
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title><%= content_for?(:title) ? yield(:title) : "Resourcing" %></title>
    <meta name="description" content="<%= content_for?(:description) ? yield(:description) : "Resourcing" %>">
    <%= stylesheet_link_tag 'application', media: 'all', 'data-turbolinks-track' => true %>
    <%= javascript_include_tag "https://js.stripe.com/v2/", 'application', 'data-turbolinks-track' => true %>
    <%= csrf_meta_tags %>
    <%= tag :meta, :value => ENV['STRIPE_PUBLISHABLE_KEY'], :name => 'stripe-key'  %>
    <%= yield :koudoku %>
  </head>
  <body>
    <header>
      <%= render 'layouts/marketing_nav' %>
    </header>

    <main role="main">
       <%= yield %>
    </main>
  </body>
</html>
